// export default function (text, placeholder) {
//   if (text === "") text.innerText = placeholder;

//   text.addEventListener("focus", (e) => {
//     if (e.target.innerHTML === placeholder) e.target.innerHTML = "";
//   });

//   text.addEventListener("blur", (e) => {
//     if (e.target.innerHTML === "") e.target.innerHTML = placeholder;
//   });
// }
